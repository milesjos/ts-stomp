

export enum StompCommand {
    ACK,
    NACK,
    ABORT,
    BEGIN,
    COMMIT,
    CONNECT,
    CONNECTED,
    DISCONNECT,
    MESSAGE,
    RECEIPT,
    SUBSCRIBE,
    UNSUBSCRIBE,
    SEND,
    ERROR
}
